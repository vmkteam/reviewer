# Code Review

## Summary
Minor issues found in error handling.

## Issues

### C1. Missing error handling
**File:** `pkg/api/handler.go:42-45`

The handler ignores the error returned by `db.Query()`.

```go
result, err := db.Query(ctx)
if err != nil {
    return fmt.Errorf("handler: %w", err)
}
```
